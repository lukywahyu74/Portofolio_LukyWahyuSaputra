# TODO - Split Portfolio HTML ke 4 file

- [x] Step 1: Update `index.html` sesuai kode HTML yang dipaste (hapus style/script inline, pindahkan referensi ke `style.css` dan `script.js`).

- [x] Step 2: Update `style.css` berisi seluruh CSS dari `<style>` pada kode HTML yang dipaste.

- [x] Step 3: Update `script.js` berisi seluruh JS dari `<script>` pada kode HTML yang dipaste.

- [x] Step 4: Update `panduan.txt` menyesuaikan struktur baru (apa yang harus diedit pada index/css/js).

- [x] Step 5: Verifikasi cepat: buka `index.html` (Live Server), cek: AOS, typing effect, progress bar, cursor glow, form submit.
